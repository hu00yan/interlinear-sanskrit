# QA: Lotus strict-key resolution after R4 honesty gate — 2026-08-26
**Verdict: build-time % was LENIENT — measured `t in _surface_index` incl. stem-prefix fallbacks the client now rejects.** Client-equivalent STRICT exact rate on saddharmapundarika: **78.2% → 80.6%** occurrences (types 62.3→65.6) after fix; per-work table `morph-dcs2-resolution.csv` (new `strict_pct` column).
Miss categories (pre-fix, of 9,979 missed occ): **(a)** sandhi-fused surface 757 occ — our tokenizer keeps fused tokens (सत्त्वाश्च, ऽभूदनिञ्जमानेन) or bare elided remnants (पि, ऽर्हन्) where DCS splits rows / writes `'pi`-style FORMs; reverse (DCS fused more than us) 246 occ; **(b)** recension/spelling variants ≈13 occ — negligible; **(c)** normalization bug = avagraha apostrophes only: build VALID_KEY rejected SLP1 `'` keys while client drops ’ — fixed, no other divergence (case/anusvāra/visarga clean); **(d)** genuinely absent 8,963 occ (90%): `सद्ध्प्`×1,230 = GRETIL ref-markers ("Saddhp_n.nn") leaked into words at ingest — TEXT-layer bug, needs owner fix; DCS covers parivartas **1–18 of 27** only (ch19–27: 71.3% via spillover); BHS/prakritized forms our Vaidya e-text retains are absent from DCS's digitization (भोति इमि तहि येहि करोन्ति…).
**Fix (pipeline/build_morph_dcs.py):** (1) `_strip_apostrophes` in `_canonical_keys`/`form_keys` on input AND generated keys — lossless, matches client `canonicalKeysFor`; (2) sandhi-fusion spans: adjacent DCS rows concatenated (≥8 chars, sentence-bounded) yield member-chain entries composed ONLY of those rows' own analyses, gated on the site's real token keys; na-final coincidental concats rejected (सत्त्वान≠sattvā+na); (3) `validate()` reports honest strict% alongside lenient%.
**Validation:** all (a)+(c) misses recovered except 98 intentionally-filtered false concats (इमु सत्त्वान तहि तस्यो…): ऽर्हन्→अर्हन्त्, ऽहं→अस्मद् 1sg, ऽसि→अस् 2sg आत्मनेपद, सहस्रकोट्यः=सहस्र+कोटी, उपायकौशल्य=उपाय+कौशल्य — eyeballed correct; sarva-guard: span keys map to their OWN rows' analyses and client trust-gate still drops stem fallbacks; HEAD-integrity 0 altered / 0 dropped (+13,411 keys, pure append); BhG ×10 identical, Raghuvamśa ×10 identical + ऽर्थे newly resolved.
**Residual honest gaps:** `सद्ध्प्` artifact inflates misses by 2.7pp until texts fixed; ch19–27 + BHS forms await DCS coverage (no fuzzy matching added, correctness > coverage); DCS-fused/site-split direction (~246 occ) unsynthesized — would need unsafe sandhi-seam guessing.
⚠️ Mid-task `.cache-dcs*/` caches were deleted externally; final shards were reconstructed additively from a complete post-merge shard snapshot + these exact filters (r1 rebuild reproduced before crash). Re-run `python3 pipeline/build_morph_dcs.py` once caches are restored to re-verify the idempotence probe end-to-end.
